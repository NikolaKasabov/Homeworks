<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link rel="stylesheet" href="/css/site.css">
  <title>Document</title>
</head>

<body>
  <header>
    <nav>
      <ul>
        <li>
          <a id="home" class="left-floated" href="/">MoneyGone</a>
        </li>
      </ul>
      <ul>
        <li>
          <a class="right-floated" href="/profile/{{userId}}">{{username}}</a>
        </li>

        <li>
          <a class="right-floated" href="/logout">Logout</a>
        </li>
      </ul>
    </nav>
  </header>


  <main>

    {{#if message}}
    <section class="notifications">
      <p class="notification-message">{{message}}</p>
    </section>
    {{/if}}

    <section class="actions">
      <form action="/refill" method="POST">
        <h2>Salary time? Refill your account amount.</h2>
        <input type="number" min="0" step="0.01" id="refill-amount" name="refill">
        <button type="submit">Refill</button>
      </form>
      <div>
        <h2>New expense comming up?</h2>
        <a href="/addExpense">Add it now</a>
      </div>
    </section>

    <hr class="separator">

    {{#if expenses}}

    <table class="expenses">
      <thead>
        <tr>
          <th>Date</th>
          <th>Merchant</th>
          <th>Amount</th>
          <th>Category</th>
          <th>Description</th>
        </tr>

      </thead>
      <tbody>

        {{#each expenses}}
        <tr>
          <td>
            <img src="/images/date.png" />
            <p>{{this.date}}</p>
          </td>
          <td>
            <p>{{this.category}}</p>
            {{#if this.report}}
            <div class="report">
              <a href="/report/{{this._id}}">Report</a>
            </div>
            {{/if}}
          </td>
          <td>
            <img src="/images/amount.png" alt="">
            <p>лв {{this.total}}</p>
          </td>
          <td>
            <img src="/images/category.png" alt="">
            <p>{{this.category}}</p>
          </td>
          <td>
            <img src="/images/hastag.png" alt="">
            <p>{{this.description}}</p>
          </td>
        </tr>
        {{/each}}

      </tbody>
    </table>

    {{else}}
    <h1 class="no-expenses">No expenses founded so far.. <span>GOOD JOB!</span></h1>
    {{/if}}

  </main>

  <footer>
    <!-- <a href="#home"> &uarr; Back to top &uarr;</a> -->
    <img src="https://softuni.bg/Content/images/about-page/softuni.png" alt="">
    <p>Software University - JavaScript Back-End September 2019 Season</p>
    <p>@2019 MoneyGone. All Rights Reserved &copy;</p>
  </footer>
</body>

</html>